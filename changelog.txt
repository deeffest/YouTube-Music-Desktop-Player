v1.27.0 is Here! (15.05.2026)
⚠️ Due to the upgrade to Qt6, you will need to log back into your account after updating the application!

- Added lyrics dialog.
- Added comments dialog.
- Added "Clean share link" plugin.
- Added support for GreaseMonkey scripts.
- Added "Invert to light theme" plugin.
- Added "Prefer system yt-dlp" setting.
- Added some tooltips for settings items in settings dialog.
- Added "windows11" style for Windows 11 (Fusion style is still using on other systems).
- Added Qt6 (PySide6 6.8.3)
- Removed Qt5 (PyQt5 5.15.11)
- Removed Picture-in-Picture.
- Fixed app icon on linux.
- Fixed Fusion palette for light/dark themes.
- Fixed Trusted Types errors with custom plugins.
- Fixed settings dialog size on linux (Cosmic DE).
- Other fixes and improvements.

v1.27.0-rc2 is Here! (12.05.2026)
- Added "Prefer system yt-dlp" setting.
- Added some tooltips for settings items in settings dialog.
- Added "windows11" style for Windows 11 (Fusion style is still using on other systems).
- Fixed settings dialog size on linux (Cosmic DE).
- Other fixes and improvements.

v1.27.0-rc1 is Here! (08.05.2026)
- Added lyrics dialog.
- Added comments dialog.
- Added "Clean share link" plugin.
- Added support for GreaseMonkey scripts.
- Added "Invert to light theme" plugin.
- Added Qt6 (PySide6 6.8.3)
- Removed Qt5 (PyQt5 5.15.11)
- Removed Picture-in-Picture.
- Fixed app icon on linux.
- Fixed Fusion palette for light/dark themes.
- Fixed Trusted Types errors with custom plugins.
- Other fixes and improvements.

v1.26.0 is Here! (06.04.2026)
- Added "Spotify" and "Genius" actions to "Search on" menu.
- Other fixes and improvements.

v1.25.0 is Here! (04.04.2026)
- Added light theme.
- Added "I'm not a kid!" plugin.
- Added custom plugins support.
- Added music recognizer using AudD API.
- Added PiP opacity and always-on-top settings.
- Added yt-dlp format settings (audio: Opus, M4A; video: MP4, WebM). 
- Added yt-dlp "Embed metadata" setting.
- Added icon color setting (system tray). 
- Added "Delete from device" button for yt-dlp, ffmpeg and deno.
- Added "Prefer system FFmpeg" and "Prefer system Deno" settings.
- Added "Restart app" action to main context menu.
- Changed toolbar style.
- Changed main context menu.
- Changed settings dialog.
- Removed proxy server functionality.
- Removed "Convert to MP3" setting for yt-dlp.
- Fixed splash screen rendering in maximized window state.
- Other fixes and improvements.

v1.25.0-rc4 is Here! (03.04.2026)
- Fixed splash screen rendering in maximized window state.
- Fixed compatibility with system FFmpeg and Deno on some Linux distributions.
- Other fixes and improvements.

v1.25.0-rc3 is Here! (02.04.2026)
- Added "Prefer system FFmpeg" and "Prefer system Deno" settings.
- Added "Restart app" action to main context menu.
- Added custom plugins support.
- Fixed focus issues in toolbar and settings dialog.
- Other fixes and improvements.

v1.25.0-rc2 is Here! (31.03.2026)
- Added music recognizer using AudD API.
- Other fixes and improvements.

v1.25.0-rc1 is Here! (29.03.2026)
- Added light theme.
- Added "I'm not a kid!" plugin.
- Added PiP opacity and always-on-top settings.
- Added yt-dlp format settings (audio: Opus, M4A; video: MP4, WebM). 
- Added yt-dlp "Embed metadata" setting.
- Added icon color setting (system tray). 
- Added "Delete from device" button for yt-dlp, ffmpeg and deno.
- Changed toolbar style.
- Changed main context menu.
- Changed settings dialog.
- Removed proxy server functionality.
- Removed "Convert to MP3" setting for yt-dlp.
- Other fixes and improvements.

v1.24.3 is Here! (26.12.2025)
- Fixed a problem where the main window would go up when switching its visibility in GNOME and some other DEs. (Take 2)
- Fixed a bug with the progress bar in the `Discord Rich Presence` plugin. (Take 2)
- Fixed an issue in the `Discord Rich Presence` plugin that caused songs with single-character titles to not be displayed.

v1.24.2 is Here! (24.12.2025)
- Fixed broken update checking after v1.24.1.
- Fixed a problem where the main window would go up when switching its visibility in GNOME and some other DEs.

v1.24.1 is Here! (24.12.2025)
- Fixed application crash when resizing window during splash screen.
- Fixed a bug with the progress bar in the `Discord Rich Presence` plugin.
- Other fixes and improvements.

v1.24.0 is Here! (22.12.2025)
- Improved codebase and UI.
- Now ytmusic observer script will not send data to the app when ads are playing.
- Now `Mini-Player` has been renamed to `Picture-in-Picture`.
- Now `Volume Up` and `Volume Down` buttons have been replaced with `Like` and `Dislike` buttons in picture-in-picture and system tray.
- Added `yt-dlp Options...` submenu to the `Download...` menu and contains:
  - `Use Cookies` - Exports your cookies and passes them to yt-dlp so that it can, for example, download your private playlists or bypass IP blocking if you use a VPN. (There is a risk of temporary account lockout, so enable as needed).
  - `Convert to MP3` - Converts the original media file (.opus) to .mp3. Needed mostly for compatibility, but messes up the already compressed quality a bit. 
  - `Auto Update` - When turned off, yt-dlp will not self-update.
- Added `Go to YouTube` action to the main context menu.
- Removed the `Ctrl+Shift+Up` and `Ctrl+Shift+Down` hotkeys from the `Playback hotkeys` plugin.
- Removed `Volume Up` and `Volume Down` buttons from Windows thumbnail buttons.
- Added `Exit full screen` action to the main context menu.
- Other fixes and improvements.

v1.24.0-rc1 is Here! (21.12.2025)
- Improved codebase and UI.
- Now ytmusic observer script will not send data to the app when ads are playing.
- Now `Mini-Player` has been renamed to `Picture-in-Picture`.
- Now `Volume Up` and `Volume Down` buttons have been replaced with `Like` and `Dislike` buttons in picture-in-picture and system tray.
- Added `yt-dlp Options...` submenu to the `Download...` menu and contains:
  - `Use Cookies` - Exports your cookies and passes them to yt-dlp so that it can, for example, download your private playlists or bypass IP blocking if you use a VPN. (There is a risk of temporary account lockout, so enable as needed).
  - `Convert to MP3` - Converts the original media file (.opus) to .mp3. Needed mostly for compatibility, but messes up the already compressed quality a bit. 
  - `Auto Update` - When turned off, yt-dlp will not self-update.
- Added `Go to YouTube` action to the main context menu.
- Removed the `Ctrl+Shift+Up` and `Ctrl+Shift+Down` hotkeys from the `Playback hotkeys` plugin.
- Removed `Volume Up` and `Volume Down` buttons from Windows thumbnail buttons.
- Other fixes and improvements.

v1.23.2 is Here! (02.12.2025)
- Fixed app closing when hiding to tray during splash.
- Fixed a critical application crash at startup caused by Service Worker.

v1.23.1 is Here! (14.11.2025)
- Fixed opening links in the browser on most Linux distributions.

v1.23.0 is Here! (14.11.2025)
- Fixed some bugs with Fluent tooltips on Xfce, so they are back.
- Fixed issues with the focus of the main window and mini-player window when switching between them on Xfce.
- Improved audio downloader - now you can download the currently playing song from any page + added support for Deno!
- Other fixes and improvements.

v1.23.0-rc1 is Here! (13.11.2025)
- Fixed some bugs with Fluent tooltips on Xfce, so they are back.
- Fixed issues with the focus of the main window and mini-player window when switching between them on Xfce.
- Improved audio downloader - now you can download the currently playing song from any page + added support for Deno!
- Other fixes and improvements.

v1.22.0 is Here! (05.11.2025)
- Fixed `is_connected AttributeError` in the `Discord Rich Presence` plugin.
- Fixed critical error `Could not initialize GLX` on recent Linux distributions (e.g. Ubuntu 25).
- Now the `Discord Rich Presence` plugin shows a progress bar.
- Fixed application crash when `Remember last opened URL` setting is disabled.
- Now the standard tooltips will be displayed on Xfce instead of Fluent tooltips due to some bugs.
- Now the folder selection dialog on Linux has a dark style.
- Fixed the `Open Folder` button not working after successfully downloading audio on most Linux distributions.
- Fixed an issue where changing the current/total track time text labels in the mini-player window would shift the playback control widgets.
- Now the track/playlist downloader on Linux uses the full yt-dlp binary instead of a Python script.
- Added a new `Hide mini player` plugin.
- Other fixes and improvements.

v1.22.0-rc3 is Here! (04.11.2025)
- Added a new `Hide mini player` plugin.

v1.22.0-rc2 is Here! (03.11.2025)
- Ubuntu Apport will no longer respond to the critical error `Could not initialize GLX`.

v1.22.0-rc1 is Here! (02.11.2025)
- Fixed `is_connected AttributeError` in the `Discord Rich Presence` plugin.
- Fixed critical error `Could not initialize GLX` on recent Linux distributions (e.g. Ubuntu 25).
- Now the `Discord Rich Presence` plugin shows a progress bar.
- Fixed application crash when `Remember last opened URL` setting is disabled.
- Now the standard tooltips will be displayed on Xfce instead of Fluent tooltips due to some bugs.
- Now the folder selection dialog on Linux has a dark style.
- Fixed the `Open Folder` button not working after successfully downloading audio on most Linux distributions.
- Fixed an issue where changing the current/total track time text labels in the mini-player window would shift the playback control widgets.
- Now the track/playlist downloader on Linux uses the full yt-dlp binary instead of a Python script.
- Other fixes and improvements.

v1.21.0 is Here! (06.10.2025)
- The `Only Audio Mode` plugin can now use HD thumbnails if they are available. (Disabled by default, can be enabled as follows: `Settings` > `Plugins` > `Only Audio Mode` > check `Use HD thumbnails`.).
- Removed `Block video` option, its functionality is now part of the `Only Audio Mode` plugin without the option to disable it.
- The `System Tray App Icon` plugin can now only be used if your system has tray support.
- Fixed yt-dlp `SSL: CERTIFICATE_VERIFY_FAILED` error on Linux.
- Fixed yt-dlp `ImportError: version OPENSSL_3.3.0 not found` on Linux.
- Added `Hide/Show Toolbar` option to the main context menu of the application.
- Now the About dialog shows the `PyQt5` and `Python` versions, as well as the `OS` name and version.
- All JS code has been updated to balance performance and quality (in particular, the `Only Audio Mode` plugin).
- Other fixes and improvements.

v1.21.0-rc2 is Here! (05.10.2025)
- Now the About dialog shows the PyQt5 and Python versions, as well as the OS name and version.
- All JS code has been updated to balance performance and quality (in particular, the `Only Audio Mode` plugin).
- Other fixes and improvements.

v1.21.0-rc1 is Here! (02.10.2025)
- The `Only Audio Mode` plugin can now use HD thumbnails if they are available. (Disabled by default, can be enabled as follows: `Settings` > `Plugins` > `Only Audio Mode` > check `Use HD thumbnails`.).
- Removed `Block video` option, its functionality is now part of the `Only Audio Mode` plugin without the option to disable it.
- The `System Tray App Icon` plugin can now only be used if your system has tray support.
- Fixed yt-dlp `SSL: CERTIFICATE_VERIFY_FAILED` error on Linux.
- Fixed yt-dlp `ImportError: version `OPENSSL_3.3.0' not found` on Linux.
- Added `Hide/Show Toolbar` option to the main context menu of the application.
- Other fixes and improvements.

v1.20.1 is Here! (23.08.2025)
- Fixed installer file for Windows. (If you have already upgraded to `v1.20.0`, go to the installation folder (by default `C:\Program Files\YTMDPlayer`) and delete the `YTMDPlayer` subfolder).

v1.20.0 is Here! (22.08.2025)
- Removed the `Windows Thumbnail Buttons` plugin - its functionality no longer depends on settings and is always enabled.  
- Significantly improved the `Ad Blocker (Skipper)` plugin.  
- Added support for `Play in Browser` and `Get App on GitHub` buttons in the `Discord Rich Presence` plugin.  
- Added the ability to stop a page reload in progress.  
- Fixed an issue where the `Discord Rich Presence` plugin continued working even when disabled.
- Improved settings dialog - namely descriptions of some settings items and messages about invalid proxy configuration.
- Other fixes and improvements.

v1.20.0-rc1 is Here! (21.08.2025)
- Removed the `Windows Thumbnail Buttons` plugin - its functionality no longer depends on settings and is always enabled.  
- Significantly improved the `Ad Blocker (Skipper)` plugin.  
- Added support for `Play in Browser` and `Get App on GitHub` buttons in the `Discord Rich Presence` plugin.  
- Added the ability to stop a page reload in progress.  
- Fixed an issue where the `Discord Rich Presence` plugin continued working even when disabled.
- Improved settings dialog - namely descriptions of some settings items and messages about invalid proxy configuration.
- Other fixes and improvements.

v1.19.0 is Here! (05.08.2025)
- Returned the message box that appeared when an unhandled error occurred in the app.
- Added URL icon to address bar.
- Now you cannot save an empty host or port exceeding 65535 in `HttpProxy` and `Socks5Proxy` in the settings dialog.
- Improved code base and exception handling in the application.
- Other fixes and improvements.

v1.18.1 is Here! (28.07.2025)
- Fixed a bug in the track/playlist downloader - `[Errno 36] File name too long`.

v1.18.0 is Here! (27.07.2025)
- Long-awaited Linux support added!
- Reworked track/playlist downloader - now based on `yt-dlp`!
- Added the long-awaited `Non-stop Music` plugin — forget about the annoying "Video paused. Continue watching?" popup!
- Removed the `Track Change Notificator` plugin.
- Replaced `Like` and `Dislike` buttons in the mini-player with `Volume Up` and `Volume Down`.
- Added `Volume Up` and `Volume Down` buttons to the system tray app icon (see the `System Tray App Icon` plugin) and to the taskbar thumbnail (see the `Windows Thumbnail Buttons` plugin).
- Now all URLs outside the explicitly allowed patterns are fully blocked instead of opening in your default browser.
- Changed the scrollbar styles on the yt music page to make them more appealing!
- Fixed reload issues - duplicate applications no longer appear and the application restarts correctly.
- Removed the message box that appeared when an unhandled error occurred in the app (now the error text is simply recorded in the log).
- Saving and opening the last URL now only works with YouTube Music URLs.
- If there is an error in the proxy server configuration, the application will now display an error message box when launched.
- Fixed issues with the `Ad Blocker (Skipper)` plugin and the ytmusic observer script.
- The `Only Audio Mode` plugin can now block video streams from loading at the network level, significantly reducing traffic consumption! (Enabled by default, can be disabled as follows: `Settings` > `Plugins` > `Only Audio Mode` > uncheck `Block video`.)
- Improved code base.
- Other fixes and improvements.

v1.18.0-rc4 is Here! (26.07.2025)
- Long-awaited Linux support added!
- Removed the `Track Change Notificator` plugin.
- Fixed an issue with the video blocker (see the `Only Audio Mode` plugin).
- Changed the scrollbar styles on the yt music page to make them more appealing!
- If there is an error in the proxy server configuration, the application will now display an error message box when launched.
- Downloading tracks/playlists via yt-dlp now occurs internally without the CMD appearing.
- Improved code base.
- Other fixes and improvements.

v1.18.0-rc3 is Here! (24.07.2025)
- Fixed reload issues - duplicate applications no longer appear and the application restarts correctly.
- Removed the message box that appeared when an unhandled error occurred in the app (now the error text is simply recorded in the log).
- Saving and opening the last URL now only works with YouTube Music URLs.
- Fixed issues with the `Ad Blocker (Skipper)` plugin and the ytmusic observer script.
- The `Only Audio Mode` plugin can now block video streams from loading at the network level, significantly reducing traffic consumption! (Enabled by default, can be disabled as follows: `Settings` > `Plugins` > `Only Audio Mode` > uncheck `Block video`.)
- Improved code base.
- Other fixes and improvements.

v1.18.0-rc2 is Here! (16.07.2025)
- Added the long-awaited `Non-stop Music` plugin — forget about the annoying "Video paused. Continue watching?" popup!
- Replaced `Like` and `Dislike` buttons in the mini-player with `Volume Up` and `Volume Down`.
- Added `Volume Up` and `Volume Down` buttons to the system tray app icon (see the `System Tray App Icon` plugin) and to the taskbar thumbnail (see the `Windows Thumbnail Buttons` plugin).
- Now all URLs outside the explicitly allowed patterns are fully blocked instead of opening in your default browser.
- Added missing descriptions for elements in the settings dialog.
- Other fixes and improvements.

v1.18.0-rc1 is Here! (23.06.2025)
- Reworked track/playlist downloader - now based on `yt-dlp`!
- Improved proxy support — system proxy is now used not only in the web engine but also in the downloader and other network components.
- To download tracks/playlists using your account (e.g., to bypass IP blocking when VPN is enabled), OAuth and manual login codes are no longer required — the downloader exports cookies directly from the web engine after you log into your YTM account.
- Improved performance of `Only Audio Mode` plugin.
- Fixed issues with logging in by adding `https://myaccount.google.com/accounts/SetOSID` and `https://gds.google.com/web/homeaddress` to the navigation whitelist.
- Other fixes and improvements.
